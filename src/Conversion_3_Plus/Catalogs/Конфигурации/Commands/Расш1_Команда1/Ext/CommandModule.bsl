
&НаКлиенте
Процедура ОбработкаКоманды(ПараметрКоманды, ПараметрыВыполненияКоманды)
	ОбработкаКомандыСервер(ПараметрКоманды);
КонецПроцедуры

&НаСервере
Процедура ОбработкаКомандыСервер(Конфигурация)
	
	Запрос = Справочники.Объекты.ПолучитьОписаниеМетеданныхКонфигурации(Конфигурация);
	
	Справочники.Конфигурации.ЗаполнитьОписаниеМетеданных(Запрос, Конфигурация);
	
КонецПроцедуры
