
#Область ПрограммныйИнтерфейс

Функция ПолучитьОписаниеМетеданныхКонфигурации(Конфигурация) Экспорт
	
	Макет = ПолучитьМакет("Расш1_Запрос_ОписаниеМетаданныхОбъектов");
	ТекстЗапроса = Макет.ПолучитьТекст();
	
	Запрос = Новый Запрос;
	Запрос.Текст = ТекстЗапроса;
	Запрос.УстановитьПараметр("Владелец", Конфигурация);
	РезультатЗапроса = Запрос.Выполнить();
	
	ВыборкаДетальныеЗаписи = РезультатЗапроса.Выбрать();
	
	
КонецФункции

#КонецОбласти

